# Speech-to-text
faster-whisper>=1.2.1

# Text-to-speech
piper-tts>=1.8.0

# Local LLM client (Ollama must be installed and running separately)
ollama>=0.6.2

# Translation — offline MT model, no LLM (see scripts/install_translate_models.py)
argostranslate>=1.11.0

# Backend API
fastapi>=0.121.2
uvicorn[standard]>=0.38.0
python-multipart>=0.0.20

# Tests: pytest for the offline unit tests (tests/test_*.py); TestClient in
# tests/test_api_smoke.py needs httpx
pytest>=8.0
httpx>=0.28.1

# Audio I/O for the CLI pipeline
sounddevice>=0.5.6
soundfile>=0.14.0
numpy>=2.5.3

# Config
python-dotenv>=1.2.3
